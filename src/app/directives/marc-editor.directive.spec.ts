import { MarcEditorDirective } from './marc-editor.directive';

describe('MarcEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcEditorDirective();
    expect(directive).toBeTruthy();
  });
});
